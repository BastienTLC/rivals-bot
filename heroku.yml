build:
  docker:
    web: ./client/Dockerfile.frontend
    backend: ./backend/Dockerfile.backend

run:
  web: /usr/sbin/nginx -g 'daemon off;'       # Pour le frontend
  backend: node /app/bin/www                  # Pour le backend
